import React from 'react';
import PageAccueil from '../components/PageAccueil';

// Composant fonctionnel pour la page principale
const PagePrincipale = () => {
    return (
        <div className="page-principale">
            <PageAccueil />
        </div>
    );
};

export default PagePrincipale;
